# 🎵 Песня «Колобок 2.0» на сайте — как это работает и как поменять

Песня кафе **уже стоит на сайте**: женский голос, весёлая, с припевом
«Колобок, Колобок, зарумянился бок… Он не просто колобок — он шаурма, браток!»

## Что сделано

| Где | Что происходит |
|---|---|
| **Колобок в шапке** (крутится) | Клик — песня пошла. Он начинает крутиться быстрее и подпрыгивать. Клик второй раз — пауза |
| **Крупный блок «Песня „Колобок 2.0“»** на главной | Большой Колобок с лицом: клик — музыка, вокруг летят ноты, снизу подпрыгивает эквалайзер, а под текстом идёт строка песни |
| **Мини-плеер внизу экрана** | Появляется, когда музыка заиграла: пауза, перемотка (можно тыкать по полоске), громкость, повтор, закрыть |
| **Экран «Заказ принят!»** | Появляется кнопка «Включить песню» — играет короткий джингл (20 секунд) |
| **Переход на другую страницу** | Браузеры запрещают автоплей, поэтому музыка ставится на паузу, а сайт подсказывает: «Нажмите на Колобка в шапке». Громкость и повтор запоминаются |

Автоплея нет специально — иначе браузеры всё равно блокируют музыку, а гости раздражаются.

## Файлы

| Файл | Размер | Что это |
|---|---|---|
| `site/assets/audio/kolobok-song.mp3` | 2,3 МБ | сама песня (96 кбит/с, 3:16) |
| `site/assets/audio/kolobok-jingle.mp3` | 264 КБ | короткий джингл для экрана заказа (с 2:48 песни) |
| `site/assets/img/song-cover.jpg` | 9 КБ | обложка (берётся из тегов исходного mp3) — крутится в плеере |
| `site/assets/js/song.js` | — | логика плеера |
| `site/assets/css/site.css` | — | стили и анимации (блок «ГИМН КАФЕ») |
| `assets-src/audio/` | — | **исходники** звука, отсюда сборщик берёт файлы |

## Как поменять песню на новую

1. Положите новый mp3 в `assets-src/audio/` под именем `kolobok-song.mp3` (замените старый).
2. Джингл: обрежьте 20 секунд и сохраните как `kolobok-jingle.mp3` — например, командой:
   ```
   ffmpeg -ss 168 -t 22 -i новый.mp3 -af "afade=t=in:d=0.6,afade=t=out:st=20.5:d=1.4" -b:a 96k kolobok-jingle.mp3
   ```
3. Для однофайловой версии (`kolobok-udomlya.html`) сборщик берёт `song-bundle.mp3` (та же песня, но лёгкая — 56 кбит/с моно). Сделать её так:
   ```
   ffmpeg -i новый.mp3 -ac 1 -b:a 56k song-bundle.mp3
   ```
4. Пересобрать сайт: `python3 build_site.py` — файлы сами разложатся по сайту, звук вшит в однофайловую версию и попадёт в архив.

## Если нужно что-то подправить

- **Громкость по умолчанию** — в `build/song.js`, строка `audio.volume = lsGet('vol', 0.55)`.
- **Строки-караоке** под Колобком — массив `LYRICS` в `build/song.js`.
- **Убрать нотку-подсказку у логотипа** — блок `.logo .kb-hint` в `build/song.css`.
- **Отключить песню совсем** — удалите из `build_site.py` строку с `song.js` (тогда исчезнут плеер, блок на главной и джингл).

## Права на музыку

Песня сгенерирована в Suno. **Для сайта кафе нужен платный тариф Suno (Pro и выше)** — на бесплатном песня только для личного прослушивания. Если тариф не оформлен, песню лучше оставить как есть до оплаты, либо заменить на трек с открытой лицензией.
